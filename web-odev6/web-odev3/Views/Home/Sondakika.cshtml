@model web_odev3.Models.ViewModel
@{
    ViewBag.Title = "Sondakika";
    Layout = "~/Views/_LayoutPage1.cshtml";
}
<div class="container">
    <div id="myCarousel" class="carousel slide" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner">


                    @{

                        string a;
                        int number = 0;



                        foreach (var item in Model.ListH.OrderByDescending(item => item.HaberID).Where(item => item.haber_turu == 1).Take(3))
                        {
                            var base64 = Convert.ToBase64String(item.haber_resmi);

                            if (number == 0)
                            {
                                a = string.Format("data:image/jpg;base64,{0}", base64);
                                number = number + 1;

                                <div class="item active">
                                    <img src='@a' alt="Los Angeles" style="width:100%;">
                                    <div class="carousel-caption">
                                    </div>
                                </div>
                            }
                            //if (number == 1)
                            //{
                            //    b = string.Format("data:image/jpg;base64,{0}", base64);
                            //    number = number + 1;
                            //}
                            //if (number == 2)
                            //{
                            //    c = string.Format("data:image/jpg;base64,{0}", base64);
                            //    number = number + 1;
                            //}
                        }
                    }

            @{

                string b;
                int number1 = 0;
                foreach (var itemm in Model.ListH.OrderByDescending(itemm => itemm.HaberID).Where(itemm => itemm.haber_turu == 1).Take(3))
                {
                    var base64 = Convert.ToBase64String(itemm.haber_resmi);
                    if (number1 == 1)
                    {
                        b = string.Format("data:image/jpg;base64,{0}", base64);
                        number1 = number1 + 1;

                        <div class="item">
                            <img src='@b' alt="Chicago" style="width:100%;">
                            <div class="carousel-caption">
                            </div>
                        </div>
                    }
                    //if (number == 2)
                    //{
                    //    c = string.Format("data:image/jpg;base64,{0}", base64);
                    //    number = number + 1;
                    //}
                }
            }

                <div class="item">
                    <img src="~/images/sondakika3.jpg" alt="New York" style="width:100%;">
                <div class="carousel-caption">
                </div>
                </div>

        </div>

        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>
