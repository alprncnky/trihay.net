@model IEnumerable<web_odev3.Models.Yorum1>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Yorum_satiri)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Haber.haber_yazisi)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Yorum_satiri)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Haber.haber_yazisi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.YorumID }) |
                @Html.ActionLink("Details", "Details", new { id=item.YorumID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.YorumID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
