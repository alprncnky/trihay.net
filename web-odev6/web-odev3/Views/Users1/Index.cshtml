@model IEnumerable<web_odev3.Models.Users1>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.userPassword)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.userPassword)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { idd=item.userEmail }) |
                @Html.ActionLink("Details", "Details", new { idd=item.userEmail }) |
                @Html.ActionLink("Delete", "Delete", new { userEmail=item.userEmail })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
